{
  "name": "recursive-promise-queue",
  "repository": {
    "type": "git",
    "url": "https://github.com/refixshow/recursive-promise-queue.git"
  },
  "version": "0.0.1",
  "description": "Promise queue, which can help with async commands across the app.",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist/ && prettier --write src/ && npm run build:esm && npm run build:cjs",
    "build:esm": "tsc",
    "build:cjs": "tsc --module CommonJS --outDir dist/cjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.14",
    "typescript": "^4.8.2"
  }
}
